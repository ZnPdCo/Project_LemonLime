name: "Check Clang Format"

on: [push, pull_request]

jobs:
  format:
    name: "Check Clang Format"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: "Install clang-format"
        run: |
          wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key 2>/dev/null | sudo apt-key add - 2>/dev/null || true
          sudo add-apt-repository -y "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-21 main" 2>/dev/null || true
          sudo apt-get update
          sudo apt-get install -y clang-format-21 2>/dev/null || sudo apt-get install -y clang-format
          sudo update-alternatives --install /usr/bin/clang-format clang-format /usr/bin/clang-format-21 100 2>/dev/null || true
          clang-format --version
      - name: "Format Codes"
        run: find src/ -type f \( -name "*.cpp" -o -name "*.h" -o -name "*.hpp" \) -exec clang-format -i {} +
      - name: Check diff
        run: git diff --exit-code HEAD
      - name: Create Pull Request
        if: failure()
        uses: peter-evans/create-pull-request@v8
        with:
          commit-message: "style: format codes"
          title: "Format codes for ${{ github.ref }}"
          labels: "style"
          assignees: "${{ github.actor }}"
          reviewers: "${{ github.actor }}"
          branch: "auto-pr/clang-format/${{ github.ref }}"
